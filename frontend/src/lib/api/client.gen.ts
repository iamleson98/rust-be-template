// This file is auto-generated by @hey-api/openapi-ts
//
// NOTE: The `baseUrl` and `credentials` overrides below are NOT auto-generated —
// they were patched so the generated client behaves like `apiFetch` in
// `../api-client.ts`:
//   1. `baseUrl: ''` → all request URLs are relative (`/api/...`) and resolve
//      against the SPA's current origin. The SPA is served by the Rust backend
//      on the same host/port, so same-origin just works in dev and prod.
//   2. `credentials: 'include'` → the httpOnly `access_token` / `refresh_token`
//      cookies set by `/api/auth/login` etc. are sent with every request,
//      so authenticated endpoints (`/api/auth/me`, `/api/bookings`, ...) work.

import { type Client, type ClientOptions, type Config, createClient, createConfig } from './client';
import type { ClientOptions as ClientOptions2 } from './types.gen';

/**
 * The `createClientConfig()` function will be called on client initialization
 * and the returned object will become the client's initial configuration.
 *
 * You may want to initialize your client this way instead of calling
 * `setConfig()`. This is useful for example if you're using Next.js
 * to ensure your client always has the correct values.
 */
export type CreateClientConfig<T extends ClientOptions = ClientOptions2> = (override?: Config<ClientOptions & T>) => Config<Required<ClientOptions> & T>;

export const client: Client = createClient(
  createConfig<ClientOptions2>({
    // Same-origin: the SPA is served by the Rust backend on :8080 in dev
    // and on the same host/port in prod. Empty baseUrl → relative URLs.
    baseUrl: '',
    // Send httpOnly JWT cookies with every request. The Rust auth flow
    // sets `access_token` + `refresh_token` cookies on login/register/refresh
    // and reads them on every authenticated endpoint.
    credentials: 'include',
  }),
);
