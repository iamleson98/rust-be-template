// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type ClientMeta, formDataBodySerializer, type Options as Options2, type RequestResult, type TDataShape } from './client';
import { client } from './client.gen';
import type { BrandDetailData, BrandDetailErrors, BrandDetailResponses, BrandsData, BrandsResponses, CampaignsData, CampaignsResponses, Cancel2Data, Cancel2Errors, Cancel2Responses, CancelData, CancelErrors, CancelPaymentData, CancelPaymentErrors, CancelPaymentResponses, CancelResponses, ChatStatsData, ChatStatsErrors, ChatStatsResponses, ClaimChannelData, ClaimChannelErrors, ClaimChannelResponses, CloseChannelData, CloseChannelErrors, CloseChannelResponses, ConfirmData, ConfirmErrors, ConfirmResponses, Create2Data, Create2Errors, Create2Responses, Create3Data, Create3Errors, Create3Responses, Create4Data, Create4Errors, Create4Responses, Create5Data, Create5Errors, Create5Responses, Create6Data, Create6Errors, Create6Responses, Create7Data, Create7Errors, Create7Responses, Create8Data, Create8Errors, Create8Responses, CreateChannelData, CreateChannelErrors, CreateChannelResponses, CreateData, CreateErrors, CreatePaymentData, CreatePaymentErrors, CreatePaymentResponses, CreatePostData, CreatePostErrors, CreatePostResponses, CreateResponses, Delete2Data, Delete2Errors, Delete2Responses, Delete3Data, Delete3Errors, Delete3Responses, Delete4Data, Delete4Errors, Delete4Responses, Delete5Data, Delete5Errors, Delete5Responses, Delete6Data, Delete6Errors, Delete6Responses, Delete7Data, Delete7Errors, Delete7Responses, DeleteAllPicturesData, DeleteAllPicturesErrors, DeleteAllPicturesResponses, DeleteData, DeleteErrors, DeletePictureData, DeletePictureErrors, DeletePictureResponses, DeletePostData, DeletePostErrors, DeletePostResponses, DeleteResponses, DeleteUserData, DeleteUserErrors, DeleteUserResponses, DetailData, DetailErrors, DetailResponses, DirectionsData, DirectionsErrors, DirectionsResponses, EmployeeLoginData, EmployeeLoginErrors, EmployeeLoginResponses, ExportData, ExportErrors, ExportResponses, Get2Data, Get2Errors, Get2Responses, GetData, GetErrors, GetPaymentData, GetPaymentErrors, GetPaymentResponses, GetPostData, GetPostResponses, GetResponses, GetStaffPresenceData, GetStaffPresenceErrors, GetStaffPresenceResponses, GetUserData, GetUserErrors, GetUserResponses, HealthData, HealthResponses, HoldData, HoldErrors, HoldResponses, IsochroneData, IsochroneErrors, IsochroneResponses, List10Data, List10Errors, List10Responses, List11Data, List11Errors, List11Responses, List12Data, List12Errors, List12Responses, List13Data, List13Responses, List14Data, List14Errors, List14Responses, List15Data, List15Responses, List16Data, List16Errors, List16Responses, List2Data, List2Errors, List2Responses, List3Data, List3Errors, List3Responses, List4Data, List4Errors, List4Responses, List5Data, List5Errors, List5Responses, List6Data, List6Errors, List6Responses, List7Data, List7Errors, List7Responses, List8Data, List8Errors, List8Responses, List9Data, List9Errors, List9Responses, ListAdminPaymentsData, ListAdminPaymentsErrors, ListAdminPaymentsResponses, ListBookingPaymentsData, ListBookingPaymentsErrors, ListBookingPaymentsResponses, ListChannelsData, ListChannelsErrors, ListChannelsResponses, ListData, ListErrors, ListExchangesData, ListExchangesErrors, ListExchangesResponses, ListMessagesData, ListMessagesErrors, ListMessagesResponses, ListPicturesData, ListPicturesErrors, ListPicturesResponses, ListPostsData, ListPostsResponses, ListResponses, ListRunsData, ListRunsErrors, ListRunsResponses, ListUsersData, ListUsersErrors, ListUsersResponses, LoginData, LoginErrors, LoginResponses, LogoutData, LogoutResponses, LookupData, LookupErrors, LookupResponses, MarkCodCollectedData, MarkCodCollectedErrors, MarkCodCollectedResponses, MarkRead2Data, MarkRead2Errors, MarkRead2Responses, MarkReadData, MarkReadErrors, MarkReadResponses, MatrixData, MatrixErrors, MatrixResponses, MeData, MeResponses, MineData, MineErrors, MineResponses, ModerateData, ModerateErrors, ModerateResponses, MomoIpnData, MomoIpnErrors, MomoIpnResponses, OauthCallbackData, OauthStartData, OauthStartErrors, PatchPictureData, PatchPictureErrors, PatchPictureResponses, PostMessageData, PostMessageErrors, PostMessageResponses, ReadyData, ReadyErrors, ReadyResponses, RecommendationsData, RecommendationsResponses, RefreshData, RefreshResponses, RegisterData, RegisterDeviceData, RegisterDeviceErrors, RegisterDeviceResponses, RegisterErrors, RegisterResponses, ReleaseChannelData, ReleaseChannelErrors, ReleaseChannelResponses, Remove2Data, Remove2Errors, Remove2Responses, Remove3Data, Remove3Errors, Remove3Responses, RemoveData, RemoveErrors, RemoveResponses, ReportVitalsData, ReportVitalsErrors, ReportVitalsResponses, ReverseData, ReverseResponses, RobotsData, RobotsResponses, RoutePicturesData, RoutePicturesResponses, RoutesData, RoutesResponses, SearchData, SearchResponses, SearchTripsData, SearchTripsResponses, ServeData, ServeErrors, ServeResponses, SetUserRoleData, SetUserRoleErrors, SetUserRoleResponses, SitemapData, SitemapResponses, Stats2Data, Stats2Responses, StatsData, StatsErrors, StatsResponses, StatusData, StatusResponses, SummaryData, SummaryErrors, SummaryResponses, SystemMetricsData, SystemMetricsErrors, SystemMetricsResponses, SystemStatusData, SystemStatusErrors, SystemStatusResponses, TagsData, TagsResponses, ToggleData, ToggleErrors, ToggleResponses, TriggerData, TriggerErrors, TriggerResponses, TripDetailData, TripDetailErrors, TripDetailResponses, UnregisterDeviceData, UnregisterDeviceErrors, UnregisterDeviceResponses, Update2Data, Update2Errors, Update2Responses, Update3Data, Update3Errors, Update3Responses, Update4Data, Update4Errors, Update4Responses, Update5Data, Update5Errors, Update5Responses, Update6Data, Update6Errors, Update6Responses, Update7Data, Update7Errors, Update7Responses, Update8Data, Update8Errors, Update8Responses, UpdateData, UpdateErrors, UpdatePaymentStatusData, UpdatePaymentStatusErrors, UpdatePaymentStatusResponses, UpdatePostData, UpdatePostErrors, UpdatePostResponses, UpdateResponses, UpdateStatusData, UpdateStatusErrors, UpdateStatusResponses, UploadPictureData, UploadPictureErrors, UploadPictureResponses, ValidateCampaignData, ValidateCampaignErrors, ValidateCampaignResponses, VnpayIpnData, VnpayIpnErrors, VnpayIpnResponses, ZalopayCallbackData, ZalopayCallbackErrors, ZalopayCallbackResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * `GET /api/admin/addresses?brandId=&q=&limit=&offset=` — list a
 * brand's addresses. `q`/`limit`/`offset` feed the searchable,
 * infinite-scroll schedule point picker; omitting `limit` returns all.
 */
export const list = <ThrowOnError extends boolean = false>(options?: Options<ListData, ThrowOnError>): RequestResult<ListResponses, ListErrors, ThrowOnError> => (options?.client ?? client).get<ListResponses, ListErrors, ThrowOnError>({ url: '/api/admin/addresses', ...options });

/**
 * `POST /api/admin/addresses` — create an address.
 */
export const create = <ThrowOnError extends boolean = false>(options: Options<CreateData, ThrowOnError>): RequestResult<CreateResponses, CreateErrors, ThrowOnError> => (options.client ?? client).post<CreateResponses, CreateErrors, ThrowOnError>({
    url: '/api/admin/addresses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/addresses/{id}` — delete an address.
 */
export const delete_ = <ThrowOnError extends boolean = false>(options: Options<DeleteData, ThrowOnError>): RequestResult<DeleteResponses, DeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteResponses, DeleteErrors, ThrowOnError>({ url: '/api/admin/addresses/{id}', ...options });

/**
 * `PUT /api/admin/addresses/{id}` — update an address.
 */
export const update = <ThrowOnError extends boolean = false>(options: Options<UpdateData, ThrowOnError>): RequestResult<UpdateResponses, UpdateErrors, ThrowOnError> => (options.client ?? client).put<UpdateResponses, UpdateErrors, ThrowOnError>({
    url: '/api/admin/addresses/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/bookings` — list bookings with admin filters.
 */
export const list2 = <ThrowOnError extends boolean = false>(options?: Options<List2Data, ThrowOnError>): RequestResult<List2Responses, List2Errors, ThrowOnError> => (options?.client ?? client).get<List2Responses, List2Errors, ThrowOnError>({ url: '/api/admin/bookings', ...options });

/**
 * `GET /api/admin/bookings/export` — CSV export.
 */
export const export_ = <ThrowOnError extends boolean = false>(options?: Options<ExportData, ThrowOnError>): RequestResult<ExportResponses, ExportErrors, ThrowOnError> => (options?.client ?? client).get<ExportResponses, ExportErrors, ThrowOnError>({ url: '/api/admin/bookings/export', ...options });

/**
 * `GET /api/admin/bookings/stats` — aggregate booking stats.
 */
export const stats = <ThrowOnError extends boolean = false>(options?: Options<StatsData, ThrowOnError>): RequestResult<StatsResponses, StatsErrors, ThrowOnError> => (options?.client ?? client).get<StatsResponses, StatsErrors, ThrowOnError>({ url: '/api/admin/bookings/stats', ...options });

/**
 * `GET /api/admin/bookings/{id}` — admin booking detail.
 */
export const get = <ThrowOnError extends boolean = false>(options: Options<GetData, ThrowOnError>): RequestResult<GetResponses, GetErrors, ThrowOnError> => (options.client ?? client).get<GetResponses, GetErrors, ThrowOnError>({ url: '/api/admin/bookings/{id}', ...options });

/**
 * `PATCH /api/admin/bookings/{id}` — update booking status (admin override).
 */
export const updateStatus = <ThrowOnError extends boolean = false>(options: Options<UpdateStatusData, ThrowOnError>): RequestResult<UpdateStatusResponses, UpdateStatusErrors, ThrowOnError> => (options.client ?? client).patch<UpdateStatusResponses, UpdateStatusErrors, ThrowOnError>({
    url: '/api/admin/bookings/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/brands` — list all brands (admin).
 */
export const list3 = <ThrowOnError extends boolean = false>(options?: Options<List3Data, ThrowOnError>): RequestResult<List3Responses, List3Errors, ThrowOnError> => (options?.client ?? client).get<List3Responses, List3Errors, ThrowOnError>({ url: '/api/admin/brands', ...options });

/**
 * `POST /api/admin/brands` — create a brand.
 */
export const create2 = <ThrowOnError extends boolean = false>(options: Options<Create2Data, ThrowOnError>): RequestResult<Create2Responses, Create2Errors, ThrowOnError> => (options.client ?? client).post<Create2Responses, Create2Errors, ThrowOnError>({
    url: '/api/admin/brands',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/brands/{id}` — delete a brand.
 */
export const delete2 = <ThrowOnError extends boolean = false>(options: Options<Delete2Data, ThrowOnError>): RequestResult<Delete2Responses, Delete2Errors, ThrowOnError> => (options.client ?? client).delete<Delete2Responses, Delete2Errors, ThrowOnError>({ url: '/api/admin/brands/{id}', ...options });

/**
 * `PUT /api/admin/brands/{id}` — update a brand.
 */
export const update2 = <ThrowOnError extends boolean = false>(options: Options<Update2Data, ThrowOnError>): RequestResult<Update2Responses, Update2Errors, ThrowOnError> => (options.client ?? client).put<Update2Responses, Update2Errors, ThrowOnError>({
    url: '/api/admin/brands/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/bus-layouts` — list bus layouts, with optional brand
 * filter and offset pagination.
 */
export const list4 = <ThrowOnError extends boolean = false>(options?: Options<List4Data, ThrowOnError>): RequestResult<List4Responses, List4Errors, ThrowOnError> => (options?.client ?? client).get<List4Responses, List4Errors, ThrowOnError>({ url: '/api/admin/bus-layouts', ...options });

/**
 * `GET /api/admin/chat/stats` — aggregate chat stats for the admin
 * dashboard's top-row cards.
 *
 * Returns counts of channels grouped by status (open / assigned /
 * closed / total) + the average first-response time in seconds.
 *
 * Server-side aggregate so the counts are accurate even when there
 * are more channels than the channel list's page size (capped at 200).
 */
export const chatStats = <ThrowOnError extends boolean = false>(options?: Options<ChatStatsData, ThrowOnError>): RequestResult<ChatStatsResponses, ChatStatsErrors, ThrowOnError> => (options?.client ?? client).get<ChatStatsResponses, ChatStatsErrors, ThrowOnError>({ url: '/api/admin/chat/stats', ...options });

/**
 * `GET /api/admin/cron-jobs` — schedules with next run + latest run.
 */
export const list5 = <ThrowOnError extends boolean = false>(options?: Options<List5Data, ThrowOnError>): RequestResult<List5Responses, List5Errors, ThrowOnError> => (options?.client ?? client).get<List5Responses, List5Errors, ThrowOnError>({ url: '/api/admin/cron-jobs', ...options });

/**
 * `GET /api/admin/cron-jobs/runs` — run history (most recent first).
 */
export const listRuns = <ThrowOnError extends boolean = false>(options?: Options<ListRunsData, ThrowOnError>): RequestResult<ListRunsResponses, ListRunsErrors, ThrowOnError> => (options?.client ?? client).get<ListRunsResponses, ListRunsErrors, ThrowOnError>({ url: '/api/admin/cron-jobs/runs', ...options });

/**
 * `PATCH /api/admin/cron-jobs/{jobType}` — enable/disable, change the
 * cadence or fire time, or re-arm the next run.
 */
export const update3 = <ThrowOnError extends boolean = false>(options: Options<Update3Data, ThrowOnError>): RequestResult<Update3Responses, Update3Errors, ThrowOnError> => (options.client ?? client).patch<Update3Responses, Update3Errors, ThrowOnError>({
    url: '/api/admin/cron-jobs/{jobType}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/admin/cron-jobs/{jobType}/cancel` — kill the queued or
 * running run of a job (the admin "Dừng" button). Cancellation is
 * cooperative: the handler observes the token at its phase boundaries
 * and finalizes its own history row; the runner ACKs (no retry).
 */
export const cancel = <ThrowOnError extends boolean = false>(options: Options<CancelData, ThrowOnError>): RequestResult<CancelResponses, CancelErrors, ThrowOnError> => (options.client ?? client).post<CancelResponses, CancelErrors, ThrowOnError>({ url: '/api/admin/cron-jobs/{jobType}/cancel', ...options });

/**
 * `POST /api/admin/cron-jobs/{jobType}/trigger` — enqueue a run now.
 */
export const trigger = <ThrowOnError extends boolean = false>(options: Options<TriggerData, ThrowOnError>): RequestResult<TriggerResponses, TriggerErrors, ThrowOnError> => (options.client ?? client).post<TriggerResponses, TriggerErrors, ThrowOnError>({ url: '/api/admin/cron-jobs/{jobType}/trigger', ...options });

/**
 * `GET /api/admin/payments` — paginated payment list.
 */
export const listAdminPayments = <ThrowOnError extends boolean = false>(options?: Options<ListAdminPaymentsData, ThrowOnError>): RequestResult<ListAdminPaymentsResponses, ListAdminPaymentsErrors, ThrowOnError> => (options?.client ?? client).get<ListAdminPaymentsResponses, ListAdminPaymentsErrors, ThrowOnError>({ url: '/api/admin/payments', ...options });

/**
 * `PATCH /api/admin/payments/{id}` — admin override of payment status.
 *
 * Used to manually mark a payment as `completed` (when the IPN failed
 * but the admin confirmed via the provider's dashboard) or `refunded`
 * (when a refund was issued out-of-band).
 */
export const updatePaymentStatus = <ThrowOnError extends boolean = false>(options: Options<UpdatePaymentStatusData, ThrowOnError>): RequestResult<UpdatePaymentStatusResponses, UpdatePaymentStatusErrors, ThrowOnError> => (options.client ?? client).patch<UpdatePaymentStatusResponses, UpdatePaymentStatusErrors, ThrowOnError>({
    url: '/api/admin/payments/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/pickup-points?routeId=` — list pickup points for a route.
 */
export const list6 = <ThrowOnError extends boolean = false>(options?: Options<List6Data, ThrowOnError>): RequestResult<List6Responses, List6Errors, ThrowOnError> => (options?.client ?? client).get<List6Responses, List6Errors, ThrowOnError>({ url: '/api/admin/pickup-points', ...options });

/**
 * `POST /api/admin/pickup-points` — create a pickup point.
 */
export const create3 = <ThrowOnError extends boolean = false>(options: Options<Create3Data, ThrowOnError>): RequestResult<Create3Responses, Create3Errors, ThrowOnError> => (options.client ?? client).post<Create3Responses, Create3Errors, ThrowOnError>({
    url: '/api/admin/pickup-points',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/pickup-points/{id}` — delete a pickup point.
 */
export const delete3 = <ThrowOnError extends boolean = false>(options: Options<Delete3Data, ThrowOnError>): RequestResult<Delete3Responses, Delete3Errors, ThrowOnError> => (options.client ?? client).delete<Delete3Responses, Delete3Errors, ThrowOnError>({ url: '/api/admin/pickup-points/{id}', ...options });

/**
 * `PUT /api/admin/pickup-points/{id}` — update a pickup point.
 */
export const update4 = <ThrowOnError extends boolean = false>(options: Options<Update4Data, ThrowOnError>): RequestResult<Update4Responses, Update4Errors, ThrowOnError> => (options.client ?? client).put<Update4Responses, Update4Errors, ThrowOnError>({
    url: '/api/admin/pickup-points/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/reviews` — list reviews with admin filters.
 */
export const list7 = <ThrowOnError extends boolean = false>(options?: Options<List7Data, ThrowOnError>): RequestResult<List7Responses, List7Errors, ThrowOnError> => (options?.client ?? client).get<List7Responses, List7Errors, ThrowOnError>({ url: '/api/admin/reviews', ...options });

/**
 * `GET /api/admin/reviews/summary` — per-brand feedback aggregates
 * (volume / status counts / avg rating) for the admin feedback page's
 * brand cards. Sorted by feedback volume descending.
 */
export const summary = <ThrowOnError extends boolean = false>(options?: Options<SummaryData, ThrowOnError>): RequestResult<SummaryResponses, SummaryErrors, ThrowOnError> => (options?.client ?? client).get<SummaryResponses, SummaryErrors, ThrowOnError>({ url: '/api/admin/reviews/summary', ...options });

/**
 * `DELETE /api/admin/reviews/{id}` — delete a review (admin override).
 */
export const delete4 = <ThrowOnError extends boolean = false>(options: Options<Delete4Data, ThrowOnError>): RequestResult<Delete4Responses, Delete4Errors, ThrowOnError> => (options.client ?? client).delete<Delete4Responses, Delete4Errors, ThrowOnError>({ url: '/api/admin/reviews/{id}', ...options });

/**
 * `PATCH /api/admin/reviews/{id}` — moderate a review (status + reply).
 */
export const moderate = <ThrowOnError extends boolean = false>(options: Options<ModerateData, ThrowOnError>): RequestResult<ModerateResponses, ModerateErrors, ThrowOnError> => (options.client ?? client).patch<ModerateResponses, ModerateErrors, ThrowOnError>({
    url: '/api/admin/reviews/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/routes` — list routes (admin), with optional brand
 * filter, search and offset pagination.
 */
export const list8 = <ThrowOnError extends boolean = false>(options?: Options<List8Data, ThrowOnError>): RequestResult<List8Responses, List8Errors, ThrowOnError> => (options?.client ?? client).get<List8Responses, List8Errors, ThrowOnError>({ url: '/api/admin/routes', ...options });

/**
 * `POST /api/admin/routes` — create a route.
 */
export const create4 = <ThrowOnError extends boolean = false>(options: Options<Create4Data, ThrowOnError>): RequestResult<Create4Responses, Create4Errors, ThrowOnError> => (options.client ?? client).post<Create4Responses, Create4Errors, ThrowOnError>({
    url: '/api/admin/routes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/routes/{id}` — delete a route.
 *
 * Garbage-collects the route's picture objects FIRST — the FK
 * cascade would drop the rows (the only index of which objects
 * exist) and orphan the objects with no way to find them again.
 */
export const delete5 = <ThrowOnError extends boolean = false>(options: Options<Delete5Data, ThrowOnError>): RequestResult<Delete5Responses, Delete5Errors, ThrowOnError> => (options.client ?? client).delete<Delete5Responses, Delete5Errors, ThrowOnError>({ url: '/api/admin/routes/{id}', ...options });

/**
 * `PUT /api/admin/routes/{id}` — update a route.
 */
export const update5 = <ThrowOnError extends boolean = false>(options: Options<Update5Data, ThrowOnError>): RequestResult<Update5Responses, Update5Errors, ThrowOnError> => (options.client ?? client).put<Update5Responses, Update5Errors, ThrowOnError>({
    url: '/api/admin/routes/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/routes/{id}/pictures` — clear the whole gallery.
 */
export const deleteAllPictures = <ThrowOnError extends boolean = false>(options: Options<DeleteAllPicturesData, ThrowOnError>): RequestResult<DeleteAllPicturesResponses, DeleteAllPicturesErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAllPicturesResponses, DeleteAllPicturesErrors, ThrowOnError>({ url: '/api/admin/routes/{id}/pictures', ...options });

/**
 * `GET /api/admin/routes/{id}/pictures` — the ordered gallery
 * (admin view; same payload as the public endpoint).
 */
export const listPictures = <ThrowOnError extends boolean = false>(options: Options<ListPicturesData, ThrowOnError>): RequestResult<ListPicturesResponses, ListPicturesErrors, ThrowOnError> => (options.client ?? client).get<ListPicturesResponses, ListPicturesErrors, ThrowOnError>({ url: '/api/admin/routes/{id}/pictures', ...options });

/**
 * `POST /api/admin/routes/{id}/pictures` — upload one picture.
 *
 * Multipart fields:
 * * `file` (required) — JPEG / PNG / WebP, ≤ 10 MiB. Validated by
 * magic bytes AND fully decoded server-side (rejects polyglot /
 * corrupt files); the client-declared Content-Type is ignored.
 * * `alt_text` (optional) — accessible description, ≤ 255 chars.
 *
 * Idempotent on content: re-uploading the exact same bytes for the
 * same route returns the existing row with `deduped: true`.
 */
export const uploadPicture = <ThrowOnError extends boolean = false>(options: Options<UploadPictureData, ThrowOnError>): RequestResult<UploadPictureResponses, UploadPictureErrors, ThrowOnError> => (options.client ?? client).post<UploadPictureResponses, UploadPictureErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/api/admin/routes/{id}/pictures',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/routes/{id}/pictures/{pictureId}` — remove one
 * picture (row + both objects).
 */
export const deletePicture = <ThrowOnError extends boolean = false>(options: Options<DeletePictureData, ThrowOnError>): RequestResult<DeletePictureResponses, DeletePictureErrors, ThrowOnError> => (options.client ?? client).delete<DeletePictureResponses, DeletePictureErrors, ThrowOnError>({ url: '/api/admin/routes/{id}/pictures/{pictureId}', ...options });

/**
 * `PATCH /api/admin/routes/{id}/pictures/{pictureId}` — reorder
 * (`sortOrder`) and/or edit `altText`.
 */
export const patchPicture = <ThrowOnError extends boolean = false>(options: Options<PatchPictureData, ThrowOnError>): RequestResult<PatchPictureResponses, PatchPictureErrors, ThrowOnError> => (options.client ?? client).patch<PatchPictureResponses, PatchPictureErrors, ThrowOnError>({
    url: '/api/admin/routes/{id}/pictures/{pictureId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/schedules?routeId=` — list schedules for a route.
 */
export const list9 = <ThrowOnError extends boolean = false>(options?: Options<List9Data, ThrowOnError>): RequestResult<List9Responses, List9Errors, ThrowOnError> => (options?.client ?? client).get<List9Responses, List9Errors, ThrowOnError>({ url: '/api/admin/schedules', ...options });

/**
 * `POST /api/admin/schedules` — create a schedule.
 */
export const create5 = <ThrowOnError extends boolean = false>(options: Options<Create5Data, ThrowOnError>): RequestResult<Create5Responses, Create5Errors, ThrowOnError> => (options.client ?? client).post<Create5Responses, Create5Errors, ThrowOnError>({
    url: '/api/admin/schedules',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/schedules/{id}` — delete a schedule.
 */
export const delete6 = <ThrowOnError extends boolean = false>(options: Options<Delete6Data, ThrowOnError>): RequestResult<Delete6Responses, Delete6Errors, ThrowOnError> => (options.client ?? client).delete<Delete6Responses, Delete6Errors, ThrowOnError>({ url: '/api/admin/schedules/{id}', ...options });

/**
 * `PUT /api/admin/schedules/{id}` — update a schedule.
 */
export const update6 = <ThrowOnError extends boolean = false>(options: Options<Update6Data, ThrowOnError>): RequestResult<Update6Responses, Update6Errors, ThrowOnError> => (options.client ?? client).put<Update6Responses, Update6Errors, ThrowOnError>({
    url: '/api/admin/schedules/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/admin/system` — system monitoring dashboard data.
 */
export const systemStatus = <ThrowOnError extends boolean = false>(options?: Options<SystemStatusData, ThrowOnError>): RequestResult<SystemStatusResponses, SystemStatusErrors, ThrowOnError> => (options?.client ?? client).get<SystemStatusResponses, SystemStatusErrors, ThrowOnError>({ url: '/api/admin/system', ...options });

/**
 * `GET /api/admin/system/metrics` — live host metrics (CPU, RAM,
 * disks, process, host info) for the admin server-monitoring page.
 *
 * Collected via the `sysinfo` crate — cross-platform by design, so
 * the same cards work against Linux, macOS and Windows backends.
 * Each scrape costs one ~200 ms CPU-sample window, so clients should
 * poll at a sane cadence (the frontend polls every 5 s). The snapshot
 * is never cached server-side — every scrape reflects live values.
 */
export const systemMetrics = <ThrowOnError extends boolean = false>(options?: Options<SystemMetricsData, ThrowOnError>): RequestResult<SystemMetricsResponses, SystemMetricsErrors, ThrowOnError> => (options?.client ?? client).get<SystemMetricsResponses, SystemMetricsErrors, ThrowOnError>({ url: '/api/admin/system/metrics', ...options });

/**
 * `GET /api/admin/vehicle-types?q=&limit=&offset=` — list the catalog
 * (label/code filter + offset pagination for the infinite picker).
 */
export const list10 = <ThrowOnError extends boolean = false>(options?: Options<List10Data, ThrowOnError>): RequestResult<List10Responses, List10Errors, ThrowOnError> => (options?.client ?? client).get<List10Responses, List10Errors, ThrowOnError>({ url: '/api/admin/vehicle-types', ...options });

/**
 * `POST /api/admin/vehicle-types` — create a vehicle type.
 */
export const create6 = <ThrowOnError extends boolean = false>(options: Options<Create6Data, ThrowOnError>): RequestResult<Create6Responses, Create6Errors, ThrowOnError> => (options.client ?? client).post<Create6Responses, Create6Errors, ThrowOnError>({
    url: '/api/admin/vehicle-types',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/admin/vehicle-types/{id}` — delete. Schedules
 * referencing the type fall back to their bus layout
 * (`ON DELETE SET NULL` semantics).
 */
export const delete7 = <ThrowOnError extends boolean = false>(options: Options<Delete7Data, ThrowOnError>): RequestResult<Delete7Responses, Delete7Errors, ThrowOnError> => (options.client ?? client).delete<Delete7Responses, Delete7Errors, ThrowOnError>({ url: '/api/admin/vehicle-types/{id}', ...options });

/**
 * `PUT /api/admin/vehicle-types/{id}` — update (patch semantics).
 */
export const update7 = <ThrowOnError extends boolean = false>(options: Options<Update7Data, ThrowOnError>): RequestResult<Update7Responses, Update7Errors, ThrowOnError> => (options.client ?? client).put<Update7Responses, Update7Errors, ThrowOnError>({
    url: '/api/admin/vehicle-types/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/auth/employee-login` — employee-only login (rejects regular users).
 */
export const employeeLogin = <ThrowOnError extends boolean = false>(options: Options<EmployeeLoginData, ThrowOnError>): RequestResult<EmployeeLoginResponses, EmployeeLoginErrors, ThrowOnError> => (options.client ?? client).post<EmployeeLoginResponses, EmployeeLoginErrors, ThrowOnError>({
    url: '/api/auth/employee-login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/auth/login` — exchange credentials for cookies.
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>): RequestResult<LoginResponses, LoginErrors, ThrowOnError> => (options.client ?? client).post<LoginResponses, LoginErrors, ThrowOnError>({
    url: '/api/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/auth/logout` — revoke the CURRENT session (default) or all
 * sessions (`{"all": true}`) + clear cookies.
 *
 * Multi-session semantics: one user may be logged in on several devices
 * simultaneously (web console + phone app). Logging out on one device
 * must NOT kill the others — the phone would otherwise silently lose
 * its push/call connectivity. Only an explicit `all: true` revokes
 * everywhere.
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>): RequestResult<LogoutResponses, unknown, ThrowOnError> => (options?.client ?? client).post<LogoutResponses, unknown, ThrowOnError>({
    url: '/api/auth/logout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * `GET /api/auth/me` — return the current authenticated user's info.
 */
export const me = <ThrowOnError extends boolean = false>(options?: Options<MeData, ThrowOnError>): RequestResult<MeResponses, unknown, ThrowOnError> => (options?.client ?? client).get<MeResponses, unknown, ThrowOnError>({ url: '/api/auth/me', ...options });

/**
 * `GET /api/auth/oauth/:provider/callback`
 *
 * Verifies the state cookie, exchanges the code for an access token,
 * fetches the user profile, and issues a session.
 */
export const oauthCallback = <ThrowOnError extends boolean = false>(options: Options<OauthCallbackData, ThrowOnError>): RequestResult<unknown, unknown, ThrowOnError> => (options.client ?? client).get<unknown, unknown, ThrowOnError>({ url: '/api/auth/oauth/{provider}/callback', ...options });

/**
 * `GET /api/auth/oauth/:provider/start`
 *
 * Generates a CSRF state, stores it in a cookie, and 302-redirects
 * the user to the OAuth provider's authorization URL.
 */
export const oauthStart = <ThrowOnError extends boolean = false>(options: Options<OauthStartData, ThrowOnError>): RequestResult<unknown, OauthStartErrors, ThrowOnError> => (options.client ?? client).get<unknown, OauthStartErrors, ThrowOnError>({ url: '/api/auth/oauth/{provider}/start', ...options });

/**
 * `POST /api/auth/refresh` — rotate refresh token, issue new access.
 *
 * Reads the refresh token from the httpOnly cookie first (preferred —
 * the token never touches JS), then falls back to the request body
 * (for non-browser clients).
 */
export const refresh = <ThrowOnError extends boolean = false>(options: Options<RefreshData, ThrowOnError>): RequestResult<RefreshResponses, unknown, ThrowOnError> => (options.client ?? client).post<RefreshResponses, unknown, ThrowOnError>({
    url: '/api/auth/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/auth/register` — create a new user account + auto-login.
 *
 * Issues auth cookies immediately on success so the user doesn't need to
 * call `/login` separately.
 */
export const register = <ThrowOnError extends boolean = false>(options: Options<RegisterData, ThrowOnError>): RequestResult<RegisterResponses, RegisterErrors, ThrowOnError> => (options.client ?? client).post<RegisterResponses, RegisterErrors, ThrowOnError>({
    url: '/api/auth/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/bookings` — list the authenticated user's bookings.
 */
export const list11 = <ThrowOnError extends boolean = false>(options?: Options<List11Data, ThrowOnError>): RequestResult<List11Responses, List11Errors, ThrowOnError> => (options?.client ?? client).get<List11Responses, List11Errors, ThrowOnError>({ url: '/api/bookings', ...options });

/**
 * `POST /api/bookings` and `POST /api/bookings/hold` — hold seats for a booking.
 */
export const hold = <ThrowOnError extends boolean = false>(options: Options<HoldData, ThrowOnError>): RequestResult<HoldResponses, HoldErrors, ThrowOnError> => (options.client ?? client).post<HoldResponses, HoldErrors, ThrowOnError>({
    url: '/api/bookings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/bookings/lookup` — lookup a booking by phone or code.
 */
export const lookup = <ThrowOnError extends boolean = false>(options?: Options<LookupData, ThrowOnError>): RequestResult<LookupResponses, LookupErrors, ThrowOnError> => (options?.client ?? client).get<LookupResponses, LookupErrors, ThrowOnError>({ url: '/api/bookings/lookup', ...options });

/**
 * `GET /api/bookings/{id}` — get booking detail.
 */
export const detail = <ThrowOnError extends boolean = false>(options: Options<DetailData, ThrowOnError>): RequestResult<DetailResponses, DetailErrors, ThrowOnError> => (options.client ?? client).get<DetailResponses, DetailErrors, ThrowOnError>({ url: '/api/bookings/{id}', ...options });

/**
 * `POST /api/bookings/{id}/cancel` — cancel a booking.
 */
export const cancel2 = <ThrowOnError extends boolean = false>(options: Options<Cancel2Data, ThrowOnError>): RequestResult<Cancel2Responses, Cancel2Errors, ThrowOnError> => (options.client ?? client).post<Cancel2Responses, Cancel2Errors, ThrowOnError>({
    url: '/api/bookings/{id}/cancel',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/bookings/{id}/confirm` — confirm a booking with payment.
 */
export const confirm = <ThrowOnError extends boolean = false>(options: Options<ConfirmData, ThrowOnError>): RequestResult<ConfirmResponses, ConfirmErrors, ThrowOnError> => (options.client ?? client).post<ConfirmResponses, ConfirmErrors, ThrowOnError>({
    url: '/api/bookings/{id}/confirm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/brands` — list brands.
 */
export const brands = <ThrowOnError extends boolean = false>(options?: Options<BrandsData, ThrowOnError>): RequestResult<BrandsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<BrandsResponses, unknown, ThrowOnError>({ url: '/api/brands', ...options });

/**
 * `GET /api/brands/{slug}` — get brand detail.
 */
export const brandDetail = <ThrowOnError extends boolean = false>(options: Options<BrandDetailData, ThrowOnError>): RequestResult<BrandDetailResponses, BrandDetailErrors, ThrowOnError> => (options.client ?? client).get<BrandDetailResponses, BrandDetailErrors, ThrowOnError>({ url: '/api/brands/{slug}', ...options });

/**
 * `GET /api/campaigns` — list campaigns.
 */
export const campaigns = <ThrowOnError extends boolean = false>(options?: Options<CampaignsData, ThrowOnError>): RequestResult<CampaignsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<CampaignsResponses, unknown, ThrowOnError>({ url: '/api/campaigns', ...options });

/**
 * `GET /api/campaigns/validate` — validate a campaign code.
 */
export const validateCampaign = <ThrowOnError extends boolean = false>(options: Options<ValidateCampaignData, ThrowOnError>): RequestResult<ValidateCampaignResponses, ValidateCampaignErrors, ThrowOnError> => (options.client ?? client).get<ValidateCampaignResponses, ValidateCampaignErrors, ThrowOnError>({ url: '/api/campaigns/validate', ...options });

/**
 * `GET /api/chat/channels` — list chat channels for the authenticated user.
 *
 * For **customers**: returns only their own channels (the ones they started).
 * For **employees**: returns the entire open-channel support queue (optionally
 * filtered by the employee's brand) so support staff see all inbound chats.
 *
 * Each channel includes the customer's `user` row (`id`, `fullName`,
 * `email`, `phone`, `avatarUrl`) so the admin's channel list can
 * display "who" without a second round-trip per channel.
 *
 * **Pagination**: `limit` (default 50, max 200) + `offset` (default
 * 0) page through the `last_message_at DESC` ordering — the initial
 * page returns the most recently active channels; scrolling the
 * admin channel list down fetches subsequent pages. The list is
 * ordered most-recent-first, so a full page means "there may be more".
 */
export const listChannels = <ThrowOnError extends boolean = false>(options?: Options<ListChannelsData, ThrowOnError>): RequestResult<ListChannelsResponses, ListChannelsErrors, ThrowOnError> => (options?.client ?? client).get<ListChannelsResponses, ListChannelsErrors, ThrowOnError>({ url: '/api/chat/channels', ...options });

/**
 * `POST /api/chat/channels` — create a new chat channel for the
 * authenticated user.
 */
export const createChannel = <ThrowOnError extends boolean = false>(options: Options<CreateChannelData, ThrowOnError>): RequestResult<CreateChannelResponses, CreateChannelErrors, ThrowOnError> => (options.client ?? client).post<CreateChannelResponses, CreateChannelErrors, ThrowOnError>({
    url: '/api/chat/channels',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/chat/channels/{id}/claim` — a staff member takes over the
 * channel. Employees get a `chat_assignment` row; admins implicitly
 * own every channel (no row, per the three-role spec).
 */
export const claimChannel = <ThrowOnError extends boolean = false>(options: Options<ClaimChannelData, ThrowOnError>): RequestResult<ClaimChannelResponses, ClaimChannelErrors, ThrowOnError> => (options.client ?? client).post<ClaimChannelResponses, ClaimChannelErrors, ThrowOnError>({ url: '/api/chat/channels/{id}/claim', ...options });

/**
 * `POST /api/chat/channels/{id}/close` — close the channel (ends any
 * assignment). Any staff member may close.
 */
export const closeChannel = <ThrowOnError extends boolean = false>(options: Options<CloseChannelData, ThrowOnError>): RequestResult<CloseChannelResponses, CloseChannelErrors, ThrowOnError> => (options.client ?? client).post<CloseChannelResponses, CloseChannelErrors, ThrowOnError>({ url: '/api/chat/channels/{id}/close', ...options });

/**
 * `GET /api/chat/channels/{id}/messages` — list messages in a channel.
 *
 * Returns messages in **DESC order (newest first)** to support
 * cursor pagination. The frontend reverses the page before rendering
 * so the oldest message is at the top + the newest at the bottom
 * (the natural chat reading order).
 *
 * - `limit` defaults to 30 (modern chat default — fast first paint).
 * Max 200.
 * - `offset` defaults to 0. `offset=30` returns the next 30 OLDER
 * messages.
 *
 * When a new message arrives via WS, the frontend invalidates this
 * query so the latest page refetches with the new message at the
 * top of the DESC page (which becomes the bottom after the
 * frontend reverses it).
 */
export const listMessages = <ThrowOnError extends boolean = false>(options: Options<ListMessagesData, ThrowOnError>): RequestResult<ListMessagesResponses, ListMessagesErrors, ThrowOnError> => (options.client ?? client).get<ListMessagesResponses, ListMessagesErrors, ThrowOnError>({ url: '/api/chat/channels/{id}/messages', ...options });

/**
 * `POST /api/chat/channels/{id}/messages` — REST fallback for posting
 * a chat message.
 *
 * Auth required: the caller must be the channel's owner (user side)
 * or an employee. Idempotent via `client_msg_id`.
 *
 * **WS broadcast**: after the message is persisted, we broadcast a
 * `message` event to the channel room so the OTHER side (customer or
 * admin) sees the reply in realtime without needing to refetch. We
 * ALSO broadcast a `channel_message` event to ALL online employees
 * (admin attention signal) when the sender is a customer.
 */
export const postMessage = <ThrowOnError extends boolean = false>(options: Options<PostMessageData, ThrowOnError>): RequestResult<PostMessageResponses, PostMessageErrors, ThrowOnError> => (options.client ?? client).post<PostMessageResponses, PostMessageErrors, ThrowOnError>({
    url: '/api/chat/channels/{id}/messages',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/chat/channels/{id}/read` — mark messages as read.
 */
export const markRead = <ThrowOnError extends boolean = false>(options: Options<MarkReadData, ThrowOnError>): RequestResult<MarkReadResponses, MarkReadErrors, ThrowOnError> => (options.client ?? client).post<MarkReadResponses, MarkReadErrors, ThrowOnError>({ url: '/api/chat/channels/{id}/read', ...options });

/**
 * `POST /api/chat/channels/{id}/release` — return the channel to the
 * open support queue. Allowed for the current assignee or any admin.
 */
export const releaseChannel = <ThrowOnError extends boolean = false>(options: Options<ReleaseChannelData, ThrowOnError>): RequestResult<ReleaseChannelResponses, ReleaseChannelErrors, ThrowOnError> => (options.client ?? client).post<ReleaseChannelResponses, ReleaseChannelErrors, ThrowOnError>({ url: '/api/chat/channels/{id}/release', ...options });

/**
 * `GET /api/media/{*key}` — serve one media object.
 */
export const serve = <ThrowOnError extends boolean = false>(options: Options<ServeData, ThrowOnError>): RequestResult<ServeResponses, ServeErrors, ThrowOnError> => (options.client ?? client).get<ServeResponses, ServeErrors, ThrowOnError>({ url: '/api/media/{key}', ...options });

/**
 * `GET /api/notifications` — list the authenticated user's notifications.
 */
export const list12 = <ThrowOnError extends boolean = false>(options?: Options<List12Data, ThrowOnError>): RequestResult<List12Responses, List12Errors, ThrowOnError> => (options?.client ?? client).get<List12Responses, List12Errors, ThrowOnError>({ url: '/api/notifications', ...options });

/**
 * `POST /api/notifications/read` — mark notifications as read.
 *
 * If the request body's `ids` array is empty, ALL of the user's unread
 * notifications are marked read. Otherwise only the listed ids are
 * marked (ownership-checked — ids not owned by the caller are ignored).
 */
export const markRead2 = <ThrowOnError extends boolean = false>(options: Options<MarkRead2Data, ThrowOnError>): RequestResult<MarkRead2Responses, MarkRead2Errors, ThrowOnError> => (options.client ?? client).post<MarkRead2Responses, MarkRead2Errors, ThrowOnError>({
    url: '/api/notifications/read',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/nullclaw/exchanges` — list NullClaw exchanges.
 *
 * **Authorization**: requires the `ADMIN_NULLCLAW_READ` permission
 * (any employee/support role). The exchanges contain the customer's
 * original prompt + the AI's completion, which may include PII such
 * as phone numbers, booking codes, and travel plans. Exposing them
 * publicly would be a data-leak (OWASP API1:2023 BOLA / API2:2023
 * Excessive Data Exposure).
 */
export const listExchanges = <ThrowOnError extends boolean = false>(options?: Options<ListExchangesData, ThrowOnError>): RequestResult<ListExchangesResponses, ListExchangesErrors, ThrowOnError> => (options?.client ?? client).get<ListExchangesResponses, ListExchangesErrors, ThrowOnError>({ url: '/api/nullclaw/exchanges', ...options });

/**
 * `GET /api/nullclaw/status` — get NullClaw provider status.
 */
export const status = <ThrowOnError extends boolean = false>(options?: Options<StatusData, ThrowOnError>): RequestResult<StatusResponses, unknown, ThrowOnError> => (options?.client ?? client).get<StatusResponses, unknown, ThrowOnError>({ url: '/api/nullclaw/status', ...options });

/**
 * `POST /api/payments` — initiate a payment for a booking.
 */
export const createPayment = <ThrowOnError extends boolean = false>(options: Options<CreatePaymentData, ThrowOnError>): RequestResult<CreatePaymentResponses, CreatePaymentErrors, ThrowOnError> => (options.client ?? client).post<CreatePaymentResponses, CreatePaymentErrors, ThrowOnError>({
    url: '/api/payments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/payments/booking/{bookingId}` — list payments for a booking.
 */
export const listBookingPayments = <ThrowOnError extends boolean = false>(options: Options<ListBookingPaymentsData, ThrowOnError>): RequestResult<ListBookingPaymentsResponses, ListBookingPaymentsErrors, ThrowOnError> => (options.client ?? client).get<ListBookingPaymentsResponses, ListBookingPaymentsErrors, ThrowOnError>({ url: '/api/payments/booking/{bookingId}', ...options });

/**
 * `POST /api/payments/ipn/momo` — MoMo IPN webhook.
 *
 * MoMo POSTs JSON to `ipnUrl`. We verify the HMAC-SHA256 signature
 * over the canonical alphabetical-key string. Response: HTTP 204
 * with body `{"code":0,"message":"..."}`.
 */
export const momoIpn = <ThrowOnError extends boolean = false>(options: Options<MomoIpnData, ThrowOnError>): RequestResult<MomoIpnResponses, MomoIpnErrors, ThrowOnError> => (options.client ?? client).post<MomoIpnResponses, MomoIpnErrors, ThrowOnError>({
    url: '/api/payments/ipn/momo',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/payments/ipn/vnpay` — VNPay IPN webhook.
 *
 * VNPay POSTs the IPN as a GET request (with all params in the URL).
 * We re-verify the HMAC-SHA512 signature and transition the payment state.
 * Response body must be JSON `{"RspCode":"00","Message":"Confirm Success"}`.
 */
export const vnpayIpn = <ThrowOnError extends boolean = false>(options: Options<VnpayIpnData, ThrowOnError>): RequestResult<VnpayIpnResponses, VnpayIpnErrors, ThrowOnError> => (options.client ?? client).get<VnpayIpnResponses, VnpayIpnErrors, ThrowOnError>({
    querySerializer: { parameters: { params: { object: { style: 'form' } } } },
    url: '/api/payments/ipn/vnpay',
    ...options
});

/**
 * `POST /api/payments/ipn/zalopay` — ZaloPay callback webhook.
 *
 * ZaloPay POSTs JSON; the `mac` field is HMAC-SHA256 over the raw body
 * bytes. We must read the body as raw bytes BEFORE deserialising to
 * avoid the JSON normaliser reformatting it (any byte-level difference
 * breaks the MAC).
 *
 * The route handler:
 * 1. Reads the raw body bytes.
 * 2. Re-deserialises them to `ZalopayCallbackPayload`.
 * 3. Passes both the parsed struct + raw bytes to the service.
 */
export const zalopayCallback = <ThrowOnError extends boolean = false>(options: Options<ZalopayCallbackData, ThrowOnError>): RequestResult<ZalopayCallbackResponses, ZalopayCallbackErrors, ThrowOnError> => (options.client ?? client).post<ZalopayCallbackResponses, ZalopayCallbackErrors, ThrowOnError>({
    url: '/api/payments/ipn/zalopay',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/payments/{id}` — get payment status (for polling).
 */
export const getPayment = <ThrowOnError extends boolean = false>(options: Options<GetPaymentData, ThrowOnError>): RequestResult<GetPaymentResponses, GetPaymentErrors, ThrowOnError> => (options.client ?? client).get<GetPaymentResponses, GetPaymentErrors, ThrowOnError>({ url: '/api/payments/{id}', ...options });

/**
 * `POST /api/payments/{id}/cancel` — cancel a pending payment.
 */
export const cancelPayment = <ThrowOnError extends boolean = false>(options: Options<CancelPaymentData, ThrowOnError>): RequestResult<CancelPaymentResponses, CancelPaymentErrors, ThrowOnError> => (options.client ?? client).post<CancelPaymentResponses, CancelPaymentErrors, ThrowOnError>({
    url: '/api/payments/{id}/cancel',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/payments/{id}/mark-cod-collected` — driver / admin marks
 * that cash was physically received for a COD booking.
 */
export const markCodCollected = <ThrowOnError extends boolean = false>(options: Options<MarkCodCollectedData, ThrowOnError>): RequestResult<MarkCodCollectedResponses, MarkCodCollectedErrors, ThrowOnError> => (options.client ?? client).post<MarkCodCollectedResponses, MarkCodCollectedErrors, ThrowOnError>({
    url: '/api/payments/{id}/mark-cod-collected',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/places` — list places.
 */
export const list13 = <ThrowOnError extends boolean = false>(options?: Options<List13Data, ThrowOnError>): RequestResult<List13Responses, unknown, ThrowOnError> => (options?.client ?? client).get<List13Responses, unknown, ThrowOnError>({ url: '/api/places', ...options });

/**
 * `GET /api/places/reverse` — reverse geocode coordinates.
 */
export const reverse = <ThrowOnError extends boolean = false>(options: Options<ReverseData, ThrowOnError>): RequestResult<ReverseResponses, unknown, ThrowOnError> => (options.client ?? client).get<ReverseResponses, unknown, ThrowOnError>({ url: '/api/places/reverse', ...options });

/**
 * `GET /api/places/search` — search places by query.
 */
export const search = <ThrowOnError extends boolean = false>(options: Options<SearchData, ThrowOnError>): RequestResult<SearchResponses, unknown, ThrowOnError> => (options.client ?? client).get<SearchResponses, unknown, ThrowOnError>({ url: '/api/places/search', ...options });

/**
 * `GET /api/posts` — list posts (public read).
 */
export const listPosts = <ThrowOnError extends boolean = false>(options?: Options<ListPostsData, ThrowOnError>): RequestResult<ListPostsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListPostsResponses, unknown, ThrowOnError>({ url: '/api/posts', ...options });

/**
 * `POST /api/posts` — create a post. Requires `posts:write`.
 */
export const createPost = <ThrowOnError extends boolean = false>(options: Options<CreatePostData, ThrowOnError>): RequestResult<CreatePostResponses, CreatePostErrors, ThrowOnError> => (options.client ?? client).post<CreatePostResponses, CreatePostErrors, ThrowOnError>({
    url: '/api/posts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/posts/{id}` — delete a post. Requires `posts:delete`.
 */
export const deletePost = <ThrowOnError extends boolean = false>(options: Options<DeletePostData, ThrowOnError>): RequestResult<DeletePostResponses, DeletePostErrors, ThrowOnError> => (options.client ?? client).delete<DeletePostResponses, DeletePostErrors, ThrowOnError>({ url: '/api/posts/{id}', ...options });

/**
 * `GET /api/posts/{id}` — get a post.
 */
export const getPost = <ThrowOnError extends boolean = false>(options: Options<GetPostData, ThrowOnError>): RequestResult<GetPostResponses, unknown, ThrowOnError> => (options.client ?? client).get<GetPostResponses, unknown, ThrowOnError>({ url: '/api/posts/{id}', ...options });

/**
 * `PATCH /api/posts/{id}` — update a post. Requires `posts:write`.
 */
export const updatePost = <ThrowOnError extends boolean = false>(options: Options<UpdatePostData, ThrowOnError>): RequestResult<UpdatePostResponses, UpdatePostErrors, ThrowOnError> => (options.client ?? client).patch<UpdatePostResponses, UpdatePostErrors, ThrowOnError>({
    url: '/api/posts/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/presence/staff` — current staff presence.
 */
export const getStaffPresence = <ThrowOnError extends boolean = false>(options?: Options<GetStaffPresenceData, ThrowOnError>): RequestResult<GetStaffPresenceResponses, GetStaffPresenceErrors, ThrowOnError> => (options?.client ?? client).get<GetStaffPresenceResponses, GetStaffPresenceErrors, ThrowOnError>({ url: '/api/presence/staff', ...options });

/**
 * `GET /api/price-alerts` — list price alerts.
 *
 * - If `phone` is provided: guest lookup (public, no auth required).
 * - Otherwise: returns the authenticated user's alerts.
 */
export const list14 = <ThrowOnError extends boolean = false>(options?: Options<List14Data, ThrowOnError>): RequestResult<List14Responses, List14Errors, ThrowOnError> => (options?.client ?? client).get<List14Responses, List14Errors, ThrowOnError>({ url: '/api/price-alerts', ...options });

/**
 * `POST /api/price-alerts` — create a price alert.
 *
 * Requires authentication. The authenticated user becomes the alert
 * owner. The `phone` field is still required (for notification
 * delivery) and must match a valid Vietnamese mobile number.
 */
export const create7 = <ThrowOnError extends boolean = false>(options: Options<Create7Data, ThrowOnError>): RequestResult<Create7Responses, Create7Errors, ThrowOnError> => (options.client ?? client).post<Create7Responses, Create7Errors, ThrowOnError>({
    url: '/api/price-alerts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/price-alerts/{id}` — cancel a price alert.
 *
 * Requires authentication. Only the alert's owner may cancel it.
 */
export const remove = <ThrowOnError extends boolean = false>(options: Options<RemoveData, ThrowOnError>): RequestResult<RemoveResponses, RemoveErrors, ThrowOnError> => (options.client ?? client).delete<RemoveResponses, RemoveErrors, ThrowOnError>({ url: '/api/price-alerts/{id}', ...options });

/**
 * `POST /api/push/devices` — register the caller's device token.
 *
 * Idempotent: re-registering a known (user, token) pair only
 * refreshes its timestamp. Any authenticated user may register; the
 * call-ring push only ever targets staff (agents), so employee
 * tokens are the ones that matter in practice.
 */
export const registerDevice = <ThrowOnError extends boolean = false>(options: Options<RegisterDeviceData, ThrowOnError>): RequestResult<RegisterDeviceResponses, RegisterDeviceErrors, ThrowOnError> => (options.client ?? client).post<RegisterDeviceResponses, RegisterDeviceErrors, ThrowOnError>({
    url: '/api/push/devices',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/push/devices/{token}` — remove one device (logout,
 * token rotation).
 */
export const unregisterDevice = <ThrowOnError extends boolean = false>(options: Options<UnregisterDeviceData, ThrowOnError>): RequestResult<UnregisterDeviceResponses, UnregisterDeviceErrors, ThrowOnError> => (options.client ?? client).delete<UnregisterDeviceResponses, UnregisterDeviceErrors, ThrowOnError>({ url: '/api/push/devices/{token}', ...options });

/**
 * `GET /api/recommendations` — get trip recommendations.
 */
export const recommendations = <ThrowOnError extends boolean = false>(options?: Options<RecommendationsData, ThrowOnError>): RequestResult<RecommendationsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RecommendationsResponses, unknown, ThrowOnError>({ url: '/api/recommendations', ...options });

/**
 * `GET /api/reviews` — public list of APPROVED reviews with optional
 * brand/route filters.
 *
 * Moderation policy (fail-closed):
 * * `status` is NOT accepted — the public list ALWAYS serves
 * `approved` rows only. Callers must never be able to enumerate
 * `pending`/`rejected`/`hidden` feedback via the public API
 * (rejected feedback may contain content the moderation team
 * deliberately suppressed).
 * * `user_id` is NOT accepted — that filter exists only on the
 * authenticated `/api/reviews/mine` (forced to the caller) and
 * the admin list. A public `user_id` filter would let anyone
 * enumerate any user's review history.
 */
export const list15 = <ThrowOnError extends boolean = false>(options?: Options<List15Data, ThrowOnError>): RequestResult<List15Responses, unknown, ThrowOnError> => (options?.client ?? client).get<List15Responses, unknown, ThrowOnError>({ url: '/api/reviews', ...options });

/**
 * `POST /api/reviews` — create a review. Requires authentication.
 */
export const create8 = <ThrowOnError extends boolean = false>(options: Options<Create8Data, ThrowOnError>): RequestResult<Create8Responses, Create8Errors, ThrowOnError> => (options.client ?? client).post<Create8Responses, Create8Errors, ThrowOnError>({
    url: '/api/reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/reviews/mine` — the authenticated user's own reviews
 * with a true `total` (server-side pagination for the account
 * feedback history page). The `user_id` scope is forced to the
 * caller — never taken from the query string.
 */
export const mine = <ThrowOnError extends boolean = false>(options?: Options<MineData, ThrowOnError>): RequestResult<MineResponses, MineErrors, ThrowOnError> => (options?.client ?? client).get<MineResponses, MineErrors, ThrowOnError>({ url: '/api/reviews/mine', ...options });

/**
 * `GET /api/reviews/tags` — public tag index over APPROVED reviews
 * only (a tag surfacing exclusively on rejected feedback would leak
 * that the moderation queue handled that topic).
 */
export const tags = <ThrowOnError extends boolean = false>(options?: Options<TagsData, ThrowOnError>): RequestResult<TagsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<TagsResponses, unknown, ThrowOnError>({ url: '/api/reviews/tags', ...options });

/**
 * `DELETE /api/reviews/{id}` — delete a review. Requires authentication.
 */
export const remove2 = <ThrowOnError extends boolean = false>(options: Options<Remove2Data, ThrowOnError>): RequestResult<Remove2Responses, Remove2Errors, ThrowOnError> => (options.client ?? client).delete<Remove2Responses, Remove2Errors, ThrowOnError>({ url: '/api/reviews/{id}', ...options });

/**
 * `GET /api/reviews/{id}` — get a review by ID.
 */
export const get2 = <ThrowOnError extends boolean = false>(options: Options<Get2Data, ThrowOnError>): RequestResult<Get2Responses, Get2Errors, ThrowOnError> => (options.client ?? client).get<Get2Responses, Get2Errors, ThrowOnError>({ url: '/api/reviews/{id}', ...options });

/**
 * `PATCH /api/reviews/{id}` — update a review. Requires authentication.
 */
export const update8 = <ThrowOnError extends boolean = false>(options: Options<Update8Data, ThrowOnError>): RequestResult<Update8Responses, Update8Errors, ThrowOnError> => (options.client ?? client).patch<Update8Responses, Update8Errors, ThrowOnError>({
    url: '/api/reviews/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/routes` — list routes.
 */
export const routes = <ThrowOnError extends boolean = false>(options?: Options<RoutesData, ThrowOnError>): RequestResult<RoutesResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RoutesResponses, unknown, ThrowOnError>({ url: '/api/routes', ...options });

/**
 * `GET /api/routes/{id}/pictures` — a route's picture gallery
 * (ordered; `sortOrder` 0 is the cover). Public read: URLs are
 * absolute against the CDN origin when configured, backend-proxied
 * otherwise. Returns an empty list for unknown routes (a route with
 * no pictures and a route that doesn't exist are the same to a
 * gallery renderer).
 */
export const routePictures = <ThrowOnError extends boolean = false>(options: Options<RoutePicturesData, ThrowOnError>): RequestResult<RoutePicturesResponses, unknown, ThrowOnError> => (options.client ?? client).get<RoutePicturesResponses, unknown, ThrowOnError>({ url: '/api/routes/{id}/pictures', ...options });

/**
 * `GET /api/routing/directions` — get directions between locations.
 */
export const directions = <ThrowOnError extends boolean = false>(options: Options<DirectionsData, ThrowOnError>): RequestResult<DirectionsResponses, DirectionsErrors, ThrowOnError> => (options.client ?? client).get<DirectionsResponses, DirectionsErrors, ThrowOnError>({ url: '/api/routing/directions', ...options });

/**
 * `GET /api/routing/isochrone` — compute isochrone contours.
 */
export const isochrone = <ThrowOnError extends boolean = false>(options: Options<IsochroneData, ThrowOnError>): RequestResult<IsochroneResponses, IsochroneErrors, ThrowOnError> => (options.client ?? client).get<IsochroneResponses, IsochroneErrors, ThrowOnError>({ url: '/api/routing/isochrone', ...options });

/**
 * `GET /api/routing/matrix` — compute distance/time matrix.
 */
export const matrix = <ThrowOnError extends boolean = false>(options: Options<MatrixData, ThrowOnError>): RequestResult<MatrixResponses, MatrixErrors, ThrowOnError> => (options.client ?? client).get<MatrixResponses, MatrixErrors, ThrowOnError>({ url: '/api/routing/matrix', ...options });

/**
 * `GET /api/search` — search trips.
 */
export const searchTrips = <ThrowOnError extends boolean = false>(options: Options<SearchTripsData, ThrowOnError>): RequestResult<SearchTripsResponses, unknown, ThrowOnError> => (options.client ?? client).get<SearchTripsResponses, unknown, ThrowOnError>({ url: '/api/search', ...options });

/**
 * `GET /api/stats` — get public stats.
 */
export const stats2 = <ThrowOnError extends boolean = false>(options?: Options<Stats2Data, ThrowOnError>): RequestResult<Stats2Responses, unknown, ThrowOnError> => (options?.client ?? client).get<Stats2Responses, unknown, ThrowOnError>({ url: '/api/stats', ...options });

/**
 * `GET /api/trips/{id}` — get trip detail.
 */
export const tripDetail = <ThrowOnError extends boolean = false>(options: Options<TripDetailData, ThrowOnError>): RequestResult<TripDetailResponses, TripDetailErrors, ThrowOnError> => (options.client ?? client).get<TripDetailResponses, TripDetailErrors, ThrowOnError>({ url: '/api/trips/{id}', ...options });

/**
 * `GET /api/users` — list users (paginated). Requires `users:read`.
 */
export const listUsers = <ThrowOnError extends boolean = false>(options?: Options<ListUsersData, ThrowOnError>): RequestResult<ListUsersResponses, ListUsersErrors, ThrowOnError> => (options?.client ?? client).get<ListUsersResponses, ListUsersErrors, ThrowOnError>({ url: '/api/users', ...options });

/**
 * `DELETE /api/users/{id}` — delete a user. Requires `users:delete`.
 */
export const deleteUser = <ThrowOnError extends boolean = false>(options: Options<DeleteUserData, ThrowOnError>): RequestResult<DeleteUserResponses, DeleteUserErrors, ThrowOnError> => (options.client ?? client).delete<DeleteUserResponses, DeleteUserErrors, ThrowOnError>({ url: '/api/users/{id}', ...options });

/**
 * `GET /api/users/{id}` — get a user. Requires `users:read`.
 */
export const getUser = <ThrowOnError extends boolean = false>(options: Options<GetUserData, ThrowOnError>): RequestResult<GetUserResponses, GetUserErrors, ThrowOnError> => (options.client ?? client).get<GetUserResponses, GetUserErrors, ThrowOnError>({ url: '/api/users/{id}', ...options });

/**
 * `PATCH /api/users/{id}/role` — change a user's role. Requires
 * `admin:users:manage-roles` (admins only by construction — the seed
 * grants it solely to the admin role).
 *
 * Guards (service layer):
 * - bot accounts can't change roles,
 * - the last human admin cannot be demoted.
 */
export const setUserRole = <ThrowOnError extends boolean = false>(options: Options<SetUserRoleData, ThrowOnError>): RequestResult<SetUserRoleResponses, SetUserRoleErrors, ThrowOnError> => (options.client ?? client).patch<SetUserRoleResponses, SetUserRoleErrors, ThrowOnError>({
    url: '/api/users/{id}/role',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `POST /api/vitals` — receive a web-vitals report from the browser.
 */
export const reportVitals = <ThrowOnError extends boolean = false>(options: Options<ReportVitalsData, ThrowOnError>): RequestResult<ReportVitalsResponses, ReportVitalsErrors, ThrowOnError> => (options.client ?? client).post<ReportVitalsResponses, ReportVitalsErrors, ThrowOnError>({
    url: '/api/vitals',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `GET /api/wishlist` — list the authenticated user's wishlist items.
 */
export const list16 = <ThrowOnError extends boolean = false>(options?: Options<List16Data, ThrowOnError>): RequestResult<List16Responses, List16Errors, ThrowOnError> => (options?.client ?? client).get<List16Responses, List16Errors, ThrowOnError>({ url: '/api/wishlist', ...options });

/**
 * `POST /api/wishlist` — toggle a route in the wishlist.
 *
 * If the route is already in the wishlist, it's removed (returns
 * `added=false`). Otherwise it's added (returns `added=true`).
 */
export const toggle = <ThrowOnError extends boolean = false>(options: Options<ToggleData, ThrowOnError>): RequestResult<ToggleResponses, ToggleErrors, ThrowOnError> => (options.client ?? client).post<ToggleResponses, ToggleErrors, ThrowOnError>({
    url: '/api/wishlist',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * `DELETE /api/wishlist/{id}` — remove a single wishlist item by id.
 */
export const remove3 = <ThrowOnError extends boolean = false>(options: Options<Remove3Data, ThrowOnError>): RequestResult<Remove3Responses, Remove3Errors, ThrowOnError> => (options.client ?? client).delete<Remove3Responses, Remove3Errors, ThrowOnError>({ url: '/api/wishlist/{id}', ...options });

/**
 * `GET /health` — liveness probe. Always returns 200 (process is up).
 */
export const health = <ThrowOnError extends boolean = false>(options?: Options<HealthData, ThrowOnError>): RequestResult<HealthResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HealthResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * `GET /ready` — readiness probe. Actually pings the DB.
 * Returns 503 if the DB is unreachable so load balancers won't route here.
 */
export const ready = <ThrowOnError extends boolean = false>(options?: Options<ReadyData, ThrowOnError>): RequestResult<ReadyResponses, ReadyErrors, ThrowOnError> => (options?.client ?? client).get<ReadyResponses, ReadyErrors, ThrowOnError>({ url: '/ready', ...options });

/**
 * `GET /robots.txt` — dynamic robots.txt that points to the sitemap.
 */
export const robots = <ThrowOnError extends boolean = false>(options?: Options<RobotsData, ThrowOnError>): RequestResult<RobotsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RobotsResponses, unknown, ThrowOnError>({ url: '/robots.txt', ...options });

/**
 * `GET /sitemap.xml` — list the public routes for SEO crawlers.
 */
export const sitemap = <ThrowOnError extends boolean = false>(options?: Options<SitemapData, ThrowOnError>): RequestResult<SitemapResponses, unknown, ThrowOnError> => (options?.client ?? client).get<SitemapResponses, unknown, ThrowOnError>({ url: '/sitemap.xml', ...options });
